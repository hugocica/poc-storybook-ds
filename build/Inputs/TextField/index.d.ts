/// <reference types="react" />
import { TTextFieldProps } from './types';
declare const TextField: ({ className, label, outlined, prefix: Prefix, suffix: Suffix, style, color, ...rest }: TTextFieldProps) => JSX.Element;
export default TextField;
